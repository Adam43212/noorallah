{
  "name": "noorallah-backend",
  "version": "1.0.0",
  "description": "Noor Allah Real Estate API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node -e \"require('./server'); setTimeout(() => fetch('http://localhost:5000/api/admin/seed', {method:'POST', headers:{'Authorization':'Bearer '+process.env.ADMIN_TOKEN}}).then(r=>r.json()).then(console.log), 2000)\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "express-validator": "^7.0.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.4",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
